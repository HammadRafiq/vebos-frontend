"use client";
import React from "react";
import { Collection } from "@sections/auth";

function CollectionPage(): JSX.Element {
  return (
    <React.Fragment>
      <Collection />
    </React.Fragment>
  );
}

export default CollectionPage;
